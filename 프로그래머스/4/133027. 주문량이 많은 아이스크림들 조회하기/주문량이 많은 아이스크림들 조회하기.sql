SELECT XX.FLAVOR
FROM 
(SELECT 
    X.FLAVOR,
    SUM(X.TOTAL_ORDER) AS TOTAL
    FROM (
SELECT
    A.FLAVOR,
    A.TOTAL_ORDER
FROM FIRST_HALF A
UNION
SELECT
    A.FLAVOR,
    A.TOTAL_ORDER
FROM JULY A
 ) X
 GROUP BY X.FLAVOR
ORDER BY TOTAL DESC
LIMIT 3
 ) XX